Add more productions to the parser for:
- various types of expressions
- Type declarations
- Events

Add corresponding classes in the ast package.

Add typechecking rules for individual expression types etc.

Add to state:
- entry, during and exit code blocks
- formals declarations (to accept values from incoming transitions)

Type declarations for struct, list, set etc.


